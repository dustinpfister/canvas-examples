
var grid = (function () {

    // public API
    return {
        // create a state
        create: function (opt) {
            opt = opt || {};
            state = {};
            return state;
        }
    }

}
    ());
